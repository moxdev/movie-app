# A Custom React App Starting Point
- Current versions used in app:
  - React 16
  - React Router v4
  - Webpack v3
  - Babel v6
- Stylus for css styles
- `yarn install && yarn start` or `npm install && npm start` to run the install and start the dev server on localhost:8080
